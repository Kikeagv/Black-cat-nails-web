import { http } from './http';

/**
 * Servicio de agendamiento y citas (se completará en BCN-19)
 */
export const citasService = {
  // Se implementará en ticket BCN-19
};

export default citasService;
