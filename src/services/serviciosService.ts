import { http } from './http';

/**
 * Servicio de catálogo de servicios (se completará en BCN-13)
 */
export const serviciosService = {
  // Se implementará en ticket BCN-13
};

export default serviciosService;
